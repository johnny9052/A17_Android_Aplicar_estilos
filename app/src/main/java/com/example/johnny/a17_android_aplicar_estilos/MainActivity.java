package com.example.johnny.a17_android_aplicar_estilos;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

public class MainActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        // En la carpeta values se define un xml style

        // Para aplicar un tema a toda una aplicación edita el archivo
        // AndroidManifest.xml y agrega el parámetro android:theme en la
        // etiqueta application:

    }
}
